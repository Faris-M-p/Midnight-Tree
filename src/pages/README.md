# src/pages/

Full-screen routes (one page ≈ one URL).

| File | Route |
|------|-------|
| `LandingPage.tsx` | `/` |
| `LoginPage.tsx` | `/login` |
| `RegisterPage.tsx` | `/register` |

The Family Tree screen currently lives inside `App.tsx` (`AppContent`) at route `/tree`.
