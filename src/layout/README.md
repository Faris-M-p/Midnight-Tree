# src/layout/

Tree positioning algorithms (pure TypeScript, no React).

| File | Purpose |
|------|---------|
| `computeFamilyTreeLayout.ts` | Calculates X/Y for every card and marriage node |

Layout is separate from drawing so hierarchy bugs are easier to fix.
